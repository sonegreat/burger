<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newsdevoured="{{devoured}}">
		{{#if devoured}}Eating TIME!{{else}}Yerp!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
